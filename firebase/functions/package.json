{
  "name": "functions",
  "version": "1.0.0",
  "description": "Firebase Functions Gen2 for ArgusOS scheduled tasks",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.72.1",
    "@google/generative-ai": "^0.24.1",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^7.0.5",
    "google-auth-library": "^9.15.0",
    "googleapis": "^144.0.0",
    "openai": "^4.104.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  },
  "private": true
}
